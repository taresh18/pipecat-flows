Introduced `role_message` (singular, `str`) as the preferred field for setting the bot's role/personality. Deprecated `role_messages` (plural) in favor of `role_message`; the old `List[Dict]` format is still supported but will be removed in 1.0.0.
